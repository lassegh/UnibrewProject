<Page x:Class="UnibrewProject.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:UnibrewProject"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:viewModel="using:UnibrewProject.ViewModel"
      xmlns:lvc="using:LiveCharts.Uwp"
      mc:Ignorable="d"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.DataContext>
        <viewModel:MainPageModel />
    </Page.DataContext>





    <StackPanel Orientation="Vertical">





        <!-- Top Grid -->
        <Grid Height="69">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition/>
                <ColumnDefinition/>

            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Background="#FF51516E">
                <TextBlock Text="QUAYSER" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Grid>
            <Grid Grid.Column="1" Background="#FF51516E"/>
            <Grid Grid.Column="2" Background="#FF51516E">
                <Ellipse HorizontalAlignment="Right" Height="50" VerticalAlignment="Center" Width="50">
                    <Ellipse.Fill>
                        <ImageBrush ImageSource="/Assets/Square44x44Logo.png"/>
                    </Ellipse.Fill>
                </Ellipse>
                <TextBlock HorizontalAlignment="Left" Height="26" Text="Michael Kastholm" TextWrapping="Wrap" VerticalAlignment="Center" Width="149" FontSize="18" Foreground="White" Margin="343,0,0,0"/>
            </Grid>

        </Grid>


        <!-- Bot Grid -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition MaxWidth="{Binding MenuWidth}">
                </ColumnDefinition>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>


            <Grid Grid.Column="0" Background="#FF51516E">
                <Button Command="{Binding MenuHideButton}">
                    <StackPanel Orientation="Horizontal">
                        <SymbolIcon Symbol="Delete" />
                        <TextBlock Text="Minimize"
                                   Visibility="{Binding MenuTextVisibility}" />
                    </StackPanel>
                </Button>
            </Grid>

            <Grid Grid.Column="1" Background="#FFE0E0EC">

            </Grid>

        </Grid>

    </StackPanel>
</Page>
